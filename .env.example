# ===========================================
# Environment
# ===========================================
NODE_ENV=local
PORT=3000
SERVICE_NAME=boilerplate

# ===========================================
# Auth0 Configuration
# Get these from Auth0 Dashboard → Applications → Your API
# ===========================================
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://your-api-identifier
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_ISSUER_BASE_URL=https://your-tenant.auth0.com

# Auth0 M2M (Machine-to-Machine) - Optional
# For internal API authentication (service-to-service)
# Create an M2M application in Auth0 for this
AUTH0_M2M_CLIENT_ID=your-m2m-client-id
AUTH0_M2M_CLIENT_SECRET=your-m2m-client-secret

# ===========================================
# AWS Configuration
# ===========================================
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Note: SSM Parameter Store is automatically used in non-local environments
# No configuration needed - set NODE_ENV and it loads from:
#   /shared/{NODE_ENV}/ (shared across services)
#   /{SERVICE_NAME}/{NODE_ENV}/ (service-specific)

# ===========================================
# PostgreSQL (via Prisma)
# ===========================================
DATABASE_URL=postgresql://user:password@localhost:5432/boilerplate?schema=public

# ===========================================
# DynamoDB
# ===========================================
DYNAMODB_TABLE_PREFIX=boilerplate
# For local development with DynamoDB Local:
# DYNAMODB_ENDPOINT=http://localhost:8000

# ===========================================
# S3 Configuration
# ===========================================
S3_BUCKET_NAME=your-bucket-name
S3_PRESIGNED_URL_EXPIRY=3600

# ===========================================
# Internal Services
# ===========================================
# Comma-separated list of internal hostnames (auth headers forwarded only to these)
INTERNAL_DOMAINS=your-internal-api.internal.com

# ===========================================
# SERVICE-SPECIFIC VARIABLES
# Add your service-specific variables below
# ===========================================

# Example: SNS Topics (one per domain)
# SNS_TOPIC_ARN_YOUR_DOMAIN=arn:aws:sns:us-east-1:123456789:your-domain-topic

# Example: Other Internal Services
# YOUR_SERVICE_API_URL=https://your-service.internal.com

# Example: Feature Flags
# YOUR_FEATURE_ENABLED=true

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=info
